"""Промпты и схема ответа для отбора моментов.

BAZA.md §13, §14, §15. Держатся отдельно от стадии по двум причинам:
их правят чаще всего остального кода, и их нужно версионировать — иначе
непонятно, на какой формулировке получен результат (§63).

Схема ответа задаётся явно, а не «просим вернуть JSON»: свободный текст
приходится разбирать регулярками, и это ровно тот класс хрупкости, от
которого предостерегает §39.
"""

from __future__ import annotations

from typing import Any

from narezka.core.scoring import TEXT_FACTORS, TEXT_PENALTIES

#: Версия промпта. Пишется в результат, чтобы через месяц было видно,
#: на какой формулировке получен отбор.
#: v4 — тип клипа убран: замер показал, что модель ставит один тип всему
#: пакету и меняет его от прогона к прогону.
PROMPT_VERSION = 4

#: Типы клипов из таксономии виральности upstream. **Не используются.**
#: Замер на трёх прогонах одного материала: модель ставит один и тот же тип
#: всем фрагментам пакета и меняет его от прогона к прогону
#: (emotional_peak → conflict → practical → story). Уточнение формулировки
#: не помогло. Поле убрано из схемы ответа: ярлык, который выглядит
#: осмысленным, но не несёт информации, хуже его отсутствия (§54).
#:
#: Оставлено как справка: если тип понадобится, его придётся спрашивать
#: по одному фрагменту на запрос — похоже, решение переносится с первого.
CLIP_TYPES = (
    "hook",            # цепляет с первой секунды
    "emotional_peak",  # сильная эмоция, крик, смех
    "revelation",      # неожиданность, раскрытие
    "conflict",        # спор, столкновение
    "victory",         # победа, удачный момент
    "failure",         # провал, поражение
    "quotable",        # фраза, которую хочется повторить
    "story",           # история с началом и концом
    "practical",       # полезное объяснение
    "other",
)

SYSTEM_PROMPT = """\
Ты отбираешь моменты для коротких вертикальных роликов из записи стрима.

Что ищем: смешное, неожиданное, сильные реакции, крики, победы, поражения, \
конфликты, споры, важные игровые моменты, мемное, эмоциональные кульминации, \
истории с началом и концом.

Главное различие, которое ты обязан проводить: **интересно зрителю** — это не \
то же самое, что **просто громко**. Громкий крик без контекста ничего не стоит \
и получает низкие оценки. Момент ценен, когда зрителю понятно, что происходит \
и почему это важно.

Для каждого фрагмента ты делаешь две вещи.

1. Уточняешь границы. Начало — там, где зритель получает контекст, а не за \
десять секунд до события. Конец — после развязки: реакции, результата, \
завершения фразы. Не обрывай на полуслове и не заканчивай на «я сейчас…». \
Опирайся на временные метки реплик и оставайся в пределах предложенного окна \
с небольшим запасом.

2. Оцениваешь по факторам, каждый от 0.0 до 1.0:
   - semantic — понятно ли, что происходит, есть ли смысл;
   - emotion — сила эмоции;
   - context — достаточно ли контекста, чтобы понять момент без остального видео;
   - completeness — законченность: есть завязка и развязка;
   - novelty — необычность, неожиданность.

И штраф, тоже от 0.0 до 1.0:
   - unresolved_ending — момент обрывается без развязки.

Про звук тебя не спрашивают: ты читаешь расшифровку и не слышишь записи. Громкость и наличие музыки измеряются отдельно.

Итоговую оценку не считай — её вычисляет программа по своим весам.

В поле explanation одним предложением по-русски объясни, почему момент \
интересен или почему нет. Без объяснения оценка бесполезна для отладки.

Отвечай на русском языке.\
"""


def response_schema() -> dict[str, Any]:
    """Схема ответа для параметра response_format."""
    factor_props = {
        name: {"type": "number", "minimum": 0, "maximum": 1} for name in TEXT_FACTORS
    }
    penalty_props = {
        name: {"type": "number", "minimum": 0, "maximum": 1} for name in TEXT_PENALTIES
    }

    return {
        "type": "json_schema",
        "json_schema": {
            "name": "clip_selection",
            "strict": True,
            "schema": {
                "type": "object",
                "additionalProperties": False,
                "required": ["clips"],
                "properties": {
                    "clips": {
                        "type": "array",
                        "items": {
                            "type": "object",
                            "additionalProperties": False,
                            "required": [
                                "index", "start", "end",
                                "factors", "penalties", "explanation",
                            ],
                            "properties": {
                                "index": {"type": "integer"},
                                "start": {"type": "number"},
                                "end": {"type": "number"},
                                "factors": {
                                    "type": "object",
                                    "additionalProperties": False,
                                    "required": list(TEXT_FACTORS),
                                    "properties": factor_props,
                                },
                                "penalties": {
                                    "type": "object",
                                    "additionalProperties": False,
                                    "required": list(TEXT_PENALTIES),
                                    "properties": penalty_props,
                                },
                                "explanation": {"type": "string"},
                            },
                        },
                    }
                },
            },
        },
    }


def format_time(seconds: float) -> str:
    total = int(seconds)
    return f"{total // 60}:{total % 60:02d}"


def render_candidate(index: int, candidate: dict[str, Any], lines: list[dict[str, Any]]) -> str:
    """Один фрагмент в виде, пригодном для чтения моделью.

    Реплики идут с метками времени: без них модель не может назвать границу
    числом, а §14 требует именно смысловой границы, а не сдвига на константу.
    """
    header = (
        f"### Фрагмент {index}\n"
        f"Предложенное окно: {candidate['start']:.1f}–{candidate['end']:.1f} с "
        f"({format_time(candidate['start'])}–{format_time(candidate['end'])})"
    )
    if not lines:
        return f"{header}\n(речи не распознано)"

    body = "\n".join(
        f"[{line['start']:.1f}] {line['text']}" for line in lines if line.get("text")
    )
    return f"{header}\n{body}"


def build_user_message(blocks: list[str]) -> str:
    return (
        "Оцени фрагменты ниже. Для каждого верни его номер, уточнённые границы "
        "в секундах, факторы, штрафы и объяснение.\n\n" + "\n\n".join(blocks)
    )


# --- тексты для публикации (§22, §23, §24) ---------------------------------

METADATA_SYSTEM_PROMPT = """\
Ты готовишь тексты для публикации короткого вертикального ролика, \
нарезанного из записи стрима.

Для каждого фрагмента дай:

1. titles — три варианта заголовка, от лучшего к худшему. Каждый до 60 \
символов. Заголовок обязан соответствовать содержанию: обещать в нём то, \
чего в ролике нет, запрещено. Не пиши капслоком, не используй «шок», \
«ты не поверишь», «что было дальше» и подобные приёмы завлечения — \
такие варианты будут отброшены.

2. description — описание в два-три предложения: что происходит и почему \
это стоит посмотреть. Без ссылок и без призывов подписаться.

3. hashtags — от трёх до восьми хэштегов по содержанию, без решётки. \
Только осмысленные, описывающие тему. Накрутка охвата вроде «рекомендации», \
«fyp», «подпишись» запрещена.

Отвечай на русском языке.\
"""


def metadata_schema() -> dict[str, Any]:
    """Схема ответа для стадии metadata."""
    return {
        "type": "json_schema",
        "json_schema": {
            "name": "clip_metadata",
            "strict": True,
            "schema": {
                "type": "object",
                "additionalProperties": False,
                "required": ["clips"],
                "properties": {
                    "clips": {
                        "type": "array",
                        "items": {
                            "type": "object",
                            "additionalProperties": False,
                            "required": ["index", "titles", "description", "hashtags"],
                            "properties": {
                                "index": {"type": "integer"},
                                "titles": {
                                    "type": "array",
                                    "items": {"type": "string"},
                                    "minItems": 1,
                                    "maxItems": 5,
                                },
                                "description": {"type": "string"},
                                "hashtags": {
                                    "type": "array",
                                    "items": {"type": "string"},
                                    "maxItems": 12,
                                },
                            },
                        },
                    }
                },
            },
        },
    }


def render_clip_for_metadata(clip: dict[str, Any], lines: list[dict[str, Any]]) -> str:
    """Фрагмент для генерации текстов.

    Кроме реплик передаётся объяснение от стадии отбора: оно уже содержит
    суть момента, и без него модель заново выводит её из сырого текста.
    """
    header = (
        f"### Фрагмент {clip['index']}\n"
        f"Длительность: {clip['end'] - clip['start']:.0f} с"
    )
    if clip.get("explanation"):
        header += f"\nЧем интересен: {clip['explanation']}"

    body = "\n".join(line["text"] for line in lines if line.get("text"))
    return f"{header}\nРечь:\n{body}" if body else f"{header}\n(речи не распознано)"


EPISODES_SYSTEM_PROMPT = """\
Ты размечаешь запись стрима на эпизоды — связные занятия с началом и концом.

Эпизод это НЕ яркий момент. Это отрезок, внутри которого человек занят одним
делом от начала до завершения: раунд от закупки до итога, просмотр видео от
«давайте глянем» до реакции, дорога из точки А в точку Б, разбор одного
события, ответы на вопросы подряд.

Признаки границы эпизода:
— смена занятия («так, теперь», «ну что, погнали», «всё, закончили»);
— завершение начатого: цель достигнута или провалена, видео досмотрено;
— долгий переход к другой теме.

ДЛИНА. Эпизод длится не меньше 5 минут, обычно 10–40. Если получается
2–3 минуты — это не занятие, а его часть: объедини с соседними в одно целое
занятие или не выделяй вовсе. Лучше три длинных эпизода, чем двадцать
коротких: из двухминутного куска компиляцию не собрать.

Не эпизод:
— отдельная шутка или всплеск эмоций — это момент, а не занятие;
— кусок короче 5 минут — это часть занятия, а не занятие;
— вся запись целиком, если человек занимался разным;
— технические паузы, настройка оборудования, разговор ни о чём.

Отвечай ТОЛЬКО массивом JSON, без пояснений вокруг:
[{"start": секунды, "end": секунды, "title": "короткое название",
  "summary": "одна фраза: чем занят и зачем это смотреть",
  "coherence": 0.0-1.0}]

`coherence` — насколько у отрезка есть начало, развитие и завершение.
0.9 — полный законченный сюжет; 0.5 — занятие есть, но без ясного финала;
0.2 — просто отрезок на одну тему.

Если связных занятий в куске нет, верни пустой массив. Пустой ответ честнее
выдуманного эпизода.
"""


def build_episodes_message(digest: str, window_start: float, window_end: float) -> str:
    """Запрос на разметку окна записи.

    Времена даются абсолютные, от начала записи: модель отвечает в них же,
    и пересчитывать её ответ не приходится — меньше места для ошибки.
    """
    return (
        f"Кусок записи с {window_start:.0f} по {window_end:.0f} секунду.\n"
        f"Времена в квадратных скобках — секунды от начала записи.\n"
        f"Отвечай в тех же абсолютных секундах.\n\n{digest}"
    )
